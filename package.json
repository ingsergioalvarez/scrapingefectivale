{
  "name": "control-vehicular",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:all": "node ./backend/scripts/dev-all.mjs",
    "build": "npm run build -w backend && npm run build -w frontend"
  }
}

